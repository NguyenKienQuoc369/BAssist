{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" }
  ],
  "buildCommand": "pip install -r requirements.txt && next build",
  "functions": {
    "api/**/*.py": {
      "runtime": "python3.11"
    }
  }
}
